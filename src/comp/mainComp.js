import React from 'react';

const MainComp = ({pageName}) => {
    return (
        <main>
         {pageName} Page
        </main>
    );
}

export default MainComp;
/*Reasiable component: we can use this component dynamicaly */